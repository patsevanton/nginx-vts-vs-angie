worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format json_combined escape=json
        '{'
            '"ts":"$time_iso8601",'
            '"remote_addr":"$remote_addr",'
            '"request_method":"$request_method",'
            '"request_uri":"$request_uri",'
            '"status":$status,'
            '"body_bytes_sent":$body_bytes_sent,'
            '"request_time":$request_time,'
            '"upstream_response_time":"$upstream_response_time",'
            '"upstream_addr":"$upstream_addr",'
            '"http_user_agent":"$http_user_agent",'
            '"http_host":"$http_host",'
            '"server_protocol":"$server_protocol",'
            '"request_length":$request_length,'
            '"bytes_sent":$bytes_sent
        '}';

    access_log /var/log/nginx/access.log json_combined;

    vhost_traffic_status_zone;
    # filter_by_host on — VTS автоматически разлагает метрики по Host запроса.
    # k6 шлёт Host: nginx-vts.benchmark.local -> метрика nginx_server_requests{host="nginx-vts.benchmark.local"}.
    # Явно server_name не прописываем (в отличие от Angie): один server_name _ ловит все Host'ы,
    # а VTS разлагает по host-лейблу без дополнительных server-блоков.
    vhost_traffic_status_filter_by_host on;

    server {
        listen 80;
        server_name _;

        location / {
            proxy_pass http://BACKEND_ADDR;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Request-ID $request_id;
        }

        location /status {
            vhost_traffic_status_display;
            vhost_traffic_status_display_format json;
        }

        location /stub_status {
            stub_status;
        }
    }
}
